apiVersion: apps/v1
kind: Deployment
metadata:
  name: enrollment-service
  namespace: jumptotech-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: enrollment-service
  template:
    metadata:
      labels:
        app: enrollment-service
    spec:
      containers:
        - name: enrollment-service
          image: local-registry:5000/jumptotech/enrollment-service:latest
          ports:
            - containerPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: enrollment-service
  namespace: jumptotech-dev
spec:
  selector:
    app: enrollment-service
  ports:
    - port: 8000
      targetPort: 8000