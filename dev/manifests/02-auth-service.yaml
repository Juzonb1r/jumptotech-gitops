apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: jumptotech-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
        - name: auth-service
          image: local-registry:5000/jumptotech/auth-service:latest
          ports:
            - containerPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: jumptotech-dev
spec:
  selector:
    app: auth-service
  ports:
    - port: 8000
      targetPort: 8000